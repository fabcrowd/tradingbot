# Kraken Spot API credentials (required for LIVE mode only).
# Create keys on your Kraken account: Spot trading ON, withdrawal OFF.
# Copy this file to ".env" in the repo root and replace the placeholders.
KRAKEN_API_KEY=your-api-key-here
KRAKEN_API_SECRET=your-api-secret-here
